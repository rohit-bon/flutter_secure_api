name: flutter_secure_api
description: "A secure and reusable API client for Flutter with token storage, authentication, interceptors, and error handling."
version: 0.0.1
repository: https://github.com/rohit-bon/flutter_secure_api
issue_tracker: https://github.com/rohit-bon/flutter_secure_api/issues
homepage:

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  dio: ^5.11.0
  flutter:
    sdk: flutter
  flutter_secure_storage: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

topics:
  - api
  - http
  - authentication
  - secure-storage
  - networking
